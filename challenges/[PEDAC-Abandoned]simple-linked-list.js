/*

Problem
- implement a linked list
- node
  data: numbers only (from 1 - 10)
  next: points to next node
- last node is "null"

- functions required
  - reverse the linked list
  - convert a linked to and from an array

- class Element
  - constructor(value, nextElement)
    - this.value = value
    - this.next
      - if nextElement is not given then set to null by default
      - else set to nextElement
     null (by default)
  - instance method: isTail()
    - return true if the elmenet's "next" property points to null
  - instance method: dataum()
    - return the value of the element
  - instance method: next()
    - reutrn the value of "next" property of element

- class SimpleLinkedList
  - instance method: size()
    - return the size of the linked-list
  - instance method: isEmpty()
    - return true if the size of linked-list is zero
  - instance method: push(value)
    - pushes an element with value of "value" to the top of the linked-list
  - instance method: pop()
    - remove the element on top of the linked-list and return the value of
    that element
  - instance method: peek()
    - return the value of the top element on the list (does not pop it)
  - instance method: head()
    - return the top element of the list (probably does not pop it)
  - instance method: toArray()
    return an array containing the element's of the linked-list
  - instance method: reverse()
    - revers the linked-list
  - static method: fromArray(arr)
    - create a linked-list. First element has value of first item in arr
    second element has value of second item in arr, etc.
    - if arr is empty that first element's value is set to? and it's "next"
    property points to null


  - contains bunch of elements. First

Example/Test Cases


Data Structure


Algorithm


Code


*/

